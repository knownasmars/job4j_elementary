package ru.job4j.loop;

import ru.job4j.loop.CheckPrimeNumber;

public class PrimeNumber {

    public static int calc(int finish) {
        int count = 0;
        for (int number = 5 % finish == 5) {
            if (CheckPrimeNumber.check(number)) {

            }
        }
        return count;
    }
}